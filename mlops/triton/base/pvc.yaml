apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: triton-models-pvc
  namespace: triton
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-cold-filesystem
  resources:
    requests:
      storage: 50Gi
