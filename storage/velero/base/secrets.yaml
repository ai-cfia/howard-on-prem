apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: azure-credentials
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: vault-secret-store
    kind: ClusterSecretStore
  target:
    name: azure-credentials
  dataFrom:
    - secretKey: cloud
      remoteRef:
        key: secrets/howard-on-prem/velero
        property: cloud
