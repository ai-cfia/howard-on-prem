apiVersion: apps/v1
kind: Deployment
metadata:
  name: nachet-swin-classifier
  namespace: nachet-dev
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: nachet-swin-classifier
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: nachet-swin-classifier
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: app
                    operator: In
                    values:
                      - nachet-swin-classifier
              topologyKey: "kubernetes.io/hostname"
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - ai-lab-3
      containers:
        - name: nachet-swin-classifier
          image: ghcr.io/ai-cfia/nachet-backend:29-azureml-swin-classifier
          imagePullPolicy: Always
          command: ["/bin/sh", "-c"]
          args: ['./startup.sh']
          # envFrom:
          #   - secretRef:
          #       name: nachet-swin-classifier-secrets
          ports:
            - containerPort: 5001
          volumeMounts:
            - name: logs
              mountPath: /app/logs
            - name: tmp
              mountPath: /tmp
          livenessProbe:
            httpGet:
              path: /
              port: 5001
            initialDelaySeconds: 120
            periodSeconds: 120
          resources:
            requests:
              cpu: "4"
              memory: "4Gi"
            limits:
              cpu: "4"
              memory: "8Gi"
          securityContext:
            runAsNonRoot: false
            runAsUser: 1000
            runAsGroup: 1000
            readOnlyRootFilesystem: false
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
      volumes:
        - name: tmp
          emptyDir:
            medium: Memory
            sizeLimit: 500Mi
        - name: logs
          emptyDir:
            medium: Memory
            sizeLimit: 500Mi
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: nachet-swin-classifier-pdb
  namespace: nachet-dev
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: nachet-swin-classifier
---
apiVersion: v1
kind: Service
metadata:
  name: nachet-swin-classifier-svc
  namespace: nachet-dev
spec:
  clusterIP: None
  selector:
    app: nachet-swin-classifier
  ports:
    - protocol: TCP
      port: 5001

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nachet-classifier-swin-15spp-triton
  namespace: nachet-dev
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: nachet-classifier-swin-15spp-triton
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: nachet-classifier-swin-15spp-triton
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: app
                    operator: In
                    values:
                      - nachet-classifier-swin-15spp-triton
              topologyKey: "kubernetes.io/hostname"
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - ai-lab-2
      imagePullSecrets:
        - name: ghcr-image-pull-secret
      containers:
        - name: nachet-classifier-swin-15spp-triton
          image: ghcr.io/ai-cfia/nachet-model-ccds/classifier-swinv2-15spp:t25.10-p2.9.1-cpu-2025112603
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          volumeMounts:
            - name: logs
              mountPath: /app/logs
            - name: tmp
              mountPath: /tmp
            - name: dshm
              mountPath: /dev/shm
          livenessProbe:
            httpGet:
              path: /v2/health/ready
              port: 8000
            initialDelaySeconds: 120
            periodSeconds: 120
          resources:
            requests:
              cpu: "4"
              memory: "4Gi"
            limits:
              cpu: "4"
              memory: "8Gi"
          securityContext:
            runAsNonRoot: false
            runAsUser: 1000
            runAsGroup: 1000
            readOnlyRootFilesystem: false
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
      volumes:
        - name: tmp
          emptyDir:
            medium: Memory
            sizeLimit: 500Mi
        - name: logs
          emptyDir:
            medium: Memory
            sizeLimit: 500Mi
        - name: dshm
          emptyDir:
            medium: Memory
            sizeLimit: 2Gi
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: nachet-classifier-swin-15spp-triton-pdb
  namespace: nachet-dev
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: nachet-classifier-swin-15spp-triton
---
apiVersion: v1
kind: Service
metadata:
  name: nachet-classifier-swin-15spp-triton-svc
  namespace: nachet-dev
spec:
  clusterIP: None
  selector:
    app: nachet-classifier-swin-15spp-triton
  ports:
    - protocol: TCP
      port: 8000

