apiVersion: v1
kind: ConfigMap
metadata:
  name: gpt-researcher-config
data:
  config.json: |
    {
      "host": "0.0.0.0",
      "port": 8000,
      "debug": false,
      "timeout": 300,
      "max_concurrent_requests": 10
    }
