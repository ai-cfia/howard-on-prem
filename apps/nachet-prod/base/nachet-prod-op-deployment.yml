---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nachet-prod-op
  namespace: nachet-prod
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: nachet-prod-op
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: nachet-prod-op
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 3000
        fsGroup: 2000
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: app
                    operator: In
                    values:
                      - nachet-prod-op
              topologyKey: "kubernetes.io/hostname"
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - ai-lab-2
      containers:
        - name: nachet-prod-op
          image: ghcr.io/ai-cfia/nachet-backend:2.3.0
          command: ["/bin/sh", "-c"]
          args: ["uv run hypercorn -b :8080 app/main:app"]
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: nachet-prod-op-secrets
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 120
            periodSeconds: 300
          resources:
            requests:
              cpu: "2"
              memory: "2Gi"
            limits:
              memory: "4Gi"
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: cache-volume
              mountPath: /.cache
            - name: tmp-volume
              mountPath: /tmp
            - name: venv-volume
              mountPath: /app/.venv
      volumes:
        - name: cache-volume
          emptyDir:
            medium: Memory
            sizeLimit: 1G
        - name: tmp-volume
          emptyDir:
            medium: Memory
            sizeLimit: 1G
        - name: venv-volume
          emptyDir:
            medium: Memory
            sizeLimit: 1G

---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: nachet-prod-op-pdb
  namespace: nachet-prod
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: nachet-prod-op

---
apiVersion: v1
kind: Service
metadata:
  name: nachet-prod-op-svc
  namespace: nachet-prod
spec:
  clusterIP: None
  selector:
    app: nachet-prod-op
  ports:
    - protocol: TCP
      port: 8080
